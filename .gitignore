target/
.idea/
*.iml
.vscode/
.classpath
.project
.settings/
.DS_Store
*.log
hs_err_pid*.log

# Pliki audio użytkownika - nie commitujemy do repo (mogą być duże + prawa autorskie).
# 01.06.2026 Beatmapy JSON juz nie sa jawnie wersjonowane.

songs/*.mp3
songs/*.wav
songs/*.aiff
songs/*.flac
songs/*.MP3
songs/*.WAV
songs/*.AIFF
songs/*.FLAC
songs/*.json

# Lokalne statystyki gracza (SQLite, generowane w runtime) - nie wersjonujemy.
stats.db

# Lokalne ustawienia użytkownika (klawisze, odliczanie) - generowane w runtime.
settings.properties

